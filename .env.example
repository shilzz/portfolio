# Example environment variables (do NOT commit real secrets)

# Session secret for cookies (Render can auto-generate via render.yaml)
SESSION_SECRET=change_this_in_production

# Public MongoDB connection string (Railway Public URL)
# Example:
# MONGODB_URI=mongodb://mongo:<PASSWORD>@yamanote.proxy.rlwy.net:31473
MONGODB_URI=

# Domain of your app (optional, used by frontend or emails)
DOMAIN=http://localhost:3000

# Optional email configuration (if you enable email features)
# EMAIL_USER=
# EMAIL_PASS=
